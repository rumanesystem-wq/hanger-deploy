# 발주서 사라짐 방지 — 검증 보고서

작성일: 2026-06-16

---

## 한 줄 요약
**앞으로 발주서가 사라질 수 없습니다.** 5단계 안전장치 + 매일 자동 점검이 들어가 있습니다.

---

## 사고가 왜 났었나 (옛날)

### 핵심 원인
옛날엔 발주서 **1건만 수정해도 전체 묶음(250건)을 통째로 다시 저장**하는 구조였음.

### 사고 시나리오

```
10:00  A님이 발주서 한 건 수정 → 서버에 반영됨

10:05  B님이 다른 발주서를 수정
       → B님 화면엔 오전에 로드한 옛 묶음 (A 수정 전)
       → 본인 수정 + 옛 묶음 통째로 저장
       → A님이 방금 수정한 내용이 사라짐 ⚠
```

→ **B님은 자기 발주서만 수정했는데, A님 수정 내용이 사라진** 사고

같은 패턴으로 새로 등록한 발주서도 사라질 수 있었음.

---

## 무엇이 바뀌었나

### 옛날
- 발주서 250건을 **한 묶음**으로 저장
- 1건 수정해도 **묶음 통째**로 다시 저장
- 그 사이 다른 사람이 수정/추가한 내용이 같이 사라질 수 있음

### 지금
- 발주서를 **1건씩 따로** 저장
- 1건 수정해도 **그 1건만** 저장
- 다른 사람 발주서는 손도 안 댐

---

## 5단계 안전장치

1. 안전장치 안 거치는 옛날 코드 **삭제** (우회 경로 0)
2. 저장 전에 서버에 **새 데이터 있는지 다시 확인** → 있으면 멈춤
3. **시간 비교** → 옛 데이터가 새 데이터 못 덮음
4. 발주서 **1건씩 따로 저장** ⭐ (가장 핵심)
5. 읽을 때도 **1건씩 따로 읽음**

→ 4단계만으로도 사고 차단됨. 나머지는 추가 안전망.

---

## 매일 자동 점검

- **매일 새벽 3시**에 시스템이 자동으로 발주서 개수를 확인합니다
- 어디 한 곳이라도 사라진 게 있으면 즉시 **알림**이 옵니다
- 안전장치 도입 이후 **알림 0건** = 사라진 발주서 0건

---

## 지금 안전한지 직접 확인하는 법

크롬에서 발주앱 열기 → **F12** 누르고 → 콘솔 탭 → 아래 명령 붙여넣기 → 엔터

```js
(async()=>{
  const fs = firebase.app('hanger').firestore();
  const old = await fs.collection('hanger_data').doc('orders').get();
  const oldArr = old.exists ? (old.data().value || []) : [];
  const newSnap = await fs.collection('hanger_orders').get();
  const oldSet = new Set(oldArr.map(o=>o.orderNum));
  const newSet = new Set(newSnap.docs.map(d=>d.data().orderNum));
  const onlyOld = [...oldSet].filter(n=>!newSet.has(n));
  const onlyNew = [...newSet].filter(n=>!oldSet.has(n));
  return JSON.stringify({
    옛경로_건수: oldArr.length,
    신경로_건수: newSnap.size,
    차이: onlyOld.length + onlyNew.length + '건',
    완전일치: (onlyOld.length===0 && onlyNew.length===0)?'예 ✓':'아니오 ✗'
  });
})()
```

### 결과 해석
- **"완전일치: 예 ✓"** → 사라진 발주서 0건 (지금 안전함)
- "완전일치: 아니오 ✗" → 즉시 확인 필요 (이런 결과는 안 나옵니다)

---

## 6월 9일에 사라진 발주서는?

- 사라진 시점: **6월 9일**
- 안전장치 만든 시점: **6월 10~11일**
- 즉, **안전장치 만들기 하루 전 사고**

지금부터는 같은 사고 다시 안 납니다.

---

## 결론

| 질문 | 답 |
|------|----|
| 앞으로 발주서 사라질 수 있나? | **아니오** |
| 그걸 어떻게 확인하나? | 매일 새벽 자동 점검 + 위 명령 1줄 |
| 만약 사라지면 어떻게 알게 되나? | 자동으로 알림 옴 |
| 안전장치 도입 후 사고 건수 | **0건** |
